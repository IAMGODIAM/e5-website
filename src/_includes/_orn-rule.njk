{# Ornament rule — accepts `mark` (<span aria-hidden="true">✦</span>, §, ¶) via include arg `mark` #}
<div class="vc-orn-rule"><span class="mk">{{ mark or '<span aria-hidden="true">✦</span>' }}</span></div>
