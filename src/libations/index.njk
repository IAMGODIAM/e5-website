---
layout: base
permalink: /libations/
title: "Libations — E5 Enclave | Honoring the Fallen, Celebrating the Risen"
description: "Libations is E5 Enclave's living tribute to those who have given their lives, their labor, and their love to the liberation and upliftment of the Black American lineage. We are a moment in a movement."
---

<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Oswald:wght@300;400;500;600;700&family=Spectral:ital,wght@0,300;0,400;0,500;1,300;1,400&display=swap"/>

<style>
  /* Libations palette — sovereign black, pan-african gold/red/green, over E5 bone */
  .lib-page {
    background: #0a0a0a;
    color: #f6f3ec;
    min-height: 100vh;
  }
  .lib-wrap { width: min(1180px, 92vw); margin: 0 auto; }
  .lib-stripe { display: flex; width: 100%; height: 5px; }
  .lib-stripe .y { flex:1; background:#f5c518; }
  .lib-stripe .r { flex:1; background:#c8342a; }
  .lib-stripe .g { flex:1; background:#1d8a47; }
  .lib-stripe.short { width: 96px; }
  .lib-stripe.med   { width: 160px; }

  /* Hero */
  .lib-hero {
    position: relative;
    min-height: 70vh;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: clamp(48px, 8vw, 120px) 0 clamp(56px, 7vw, 100px);
    overflow: hidden;
    isolation: isolate;
  }
  .lib-hero::before {
    content: "";
    position: absolute; inset: 0;
    background: linear-gradient(135deg, #0a0a0a 0%, #1a1208 40%, #0f0f09 100%);
    z-index: -1;
  }
  /* Subtle grain texture */
  .lib-hero::after {
    content: "";
    position: absolute; inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
    z-index: -1;
    opacity: 0.4;
  }
  .lib-hero-eyebrow {
    font-family: 'Oswald', sans-serif;
    text-transform: uppercase;
    letter-spacing: 0.4em;
    font-size: 11px;
    color: #f5c518;
    margin-bottom: 24px;
  }
  .lib-hero-title {
    font-family: 'Oswald', Impact, sans-serif;
    font-weight: 700;
    text-transform: uppercase;
    font-size: clamp(72px, 14vw, 200px);
    line-height: 0.87;
    letter-spacing: -0.01em;
    color: #f6f3ec;
    margin: 0 0 32px;
  }
  .lib-hero-deck {
    font-family: 'Spectral', Georgia, serif;
    font-size: clamp(18px, 1.8vw, 24px);
    font-weight: 300;
    font-style: italic;
    color: #c9c4b8;
    max-width: 52ch;
    line-height: 1.5;
    margin: 0 0 40px;
  }
  .lib-hero-creed {
    font-family: 'Oswald', sans-serif;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-size: 12px;
    color: rgba(246,243,236,0.4);
    margin-top: 16px;
  }

  /* Doctrine bar */
  .lib-doctrine {
    background: #111110;
    border-top: 1px solid rgba(246,243,236,0.08);
    border-bottom: 1px solid rgba(246,243,236,0.08);
    padding: clamp(40px,5vw,72px) 0;
  }
  .lib-doctrine-inner {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: clamp(32px,5vw,80px);
    align-items: center;
  }
  @media(max-width:720px){ .lib-doctrine-inner { grid-template-columns:1fr; } }
  .lib-doctrine-label {
    font-family:'Oswald',sans-serif;
    text-transform:uppercase;
    letter-spacing:0.3em;
    font-size:11px;
    color:#f5c518;
    margin-bottom:16px;
  }
  .lib-doctrine-title {
    font-family:'Oswald',sans-serif;
    font-weight:700;
    text-transform:uppercase;
    font-size:clamp(32px,4vw,52px);
    line-height:0.95;
    color:#f6f3ec;
    margin:0;
  }
  .lib-doctrine-body {
    font-family:'Spectral',Georgia,serif;
    font-size:clamp(17px,1.5vw,21px);
    line-height:1.65;
    color:#c9c4b8;
    font-weight:300;
  }
  .lib-doctrine-body p { margin:0 0 1em; }
  .lib-doctrine-body p:last-child { margin:0; }

  /* Tribute grid */
  .lib-grid-section {
    padding: clamp(64px,9vw,120px) 0;
  }
  .lib-grid-header {
    display:flex;
    flex-direction:column;
    gap:14px;
    margin-bottom:clamp(40px,6vw,80px);
  }
  .lib-grid-eyebrow {
    font-family:'Oswald',sans-serif;
    text-transform:uppercase;
    letter-spacing:0.3em;
    font-size:11px;
    color:#f5c518;
  }
  .lib-grid-title {
    font-family:'Oswald',sans-serif;
    font-weight:700;
    text-transform:uppercase;
    font-size:clamp(36px,5vw,72px);
    line-height:0.92;
    color:#f6f3ec;
    margin:0;
  }
  .lib-grid {
    display:grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap:2px;
  }

  /* Tribute card — featured (first) */
  .lib-card {
    position:relative;
    display:flex;
    flex-direction:column;
    justify-content:flex-end;
    min-height:480px;
    overflow:hidden;
    background:#111;
    cursor:pointer;
    text-decoration:none;
    border:none;
  }
  .lib-card::before {
    content:"";
    position:absolute;inset:0;
    background:linear-gradient(180deg, rgba(10,10,10,0) 20%, rgba(10,10,10,0.6) 55%, rgba(10,10,10,0.96) 100%);
    z-index:1;
    transition: opacity 0.35s;
  }
  .lib-card:hover::before { opacity:0.75; }
  .lib-card img {
    position:absolute;inset:0;
    width:100%; height:100%;
    object-fit:cover;
    transition: transform 0.6s cubic-bezier(0.25,0.46,0.45,0.94);
    filter:saturate(0.9);
  }
  .lib-card:hover img { transform:scale(1.04); }
  .lib-card-body {
    position:relative;
    z-index:2;
    padding:clamp(24px,3vw,40px);
  }
  .lib-card-stripe { margin-bottom:16px; }
  .lib-card-eyebrow {
    font-family:'Oswald',sans-serif;
    text-transform:uppercase;
    letter-spacing:0.26em;
    font-size:10px;
    color:#f5c518;
    margin-bottom:8px;
  }
  .lib-card-name {
    font-family:'Oswald',sans-serif;
    font-weight:700;
    text-transform:uppercase;
    font-size:clamp(28px,4vw,52px);
    line-height:0.92;
    color:#f6f3ec;
    margin:0 0 10px;
  }
  .lib-card-role {
    font-family:'Oswald',sans-serif;
    text-transform:uppercase;
    letter-spacing:0.18em;
    font-size:11px;
    color:#c9c4b8;
    margin-bottom:14px;
  }
  .lib-card-quote {
    font-family:'Spectral',Georgia,serif;
    font-style:italic;
    font-size:clamp(14px,1.4vw,17px);
    color:#c9c4b8;
    line-height:1.5;
    max-width:38ch;
    font-weight:300;
    margin:0 0 20px;
  }
  .lib-card-cta {
    display:inline-block;
    font-family:'Oswald',sans-serif;
    text-transform:uppercase;
    letter-spacing:0.22em;
    font-size:10px;
    color:#f5c518;
    padding:8px 0;
    border-bottom:1px solid rgba(245,197,24,0.4);
  }
  .lib-card-date {
    position:absolute;
    top:clamp(20px,2.5vw,32px);
    right:clamp(20px,2.5vw,32px);
    z-index:2;
    font-family:'Oswald',sans-serif;
    text-transform:uppercase;
    letter-spacing:0.2em;
    font-size:10px;
    color:rgba(246,243,236,0.45);
  }

  /* Placeholder "coming soon" card */
  .lib-card-placeholder {
    min-height:340px;
    background: linear-gradient(135deg, #141413, #0d0d0c);
    border:1px dashed rgba(246,243,236,0.1);
    display:flex;
    flex-direction:column;
    align-items:center;
    justify-content:center;
    gap:16px;
    padding:40px;
  }
  .lib-card-placeholder .ph-mark {
    font-family:'Oswald',sans-serif;
    font-size:56px;
    font-weight:700;
    color:rgba(246,243,236,0.06);
    line-height:1;
  }
  .lib-card-placeholder .ph-label {
    font-family:'Oswald',sans-serif;
    text-transform:uppercase;
    letter-spacing:0.3em;
    font-size:10px;
    color:rgba(246,243,236,0.25);
    text-align:center;
  }

  /* Creed section */
  .lib-creed {
    padding: clamp(64px,9vw,120px) 0;
    border-top: 1px solid rgba(246,243,236,0.08);
  }
  .lib-creed-inner {
    max-width:68ch;
    margin:0 auto;
    text-align:center;
  }
  .lib-creed blockquote {
    font-family:'Oswald',sans-serif;
    font-weight:700;
    text-transform:uppercase;
    font-size:clamp(28px,4vw,52px);
    line-height:0.97;
    color:#f6f3ec;
    margin:0 0 32px;
  }
  .lib-creed cite {
    font-family:'Spectral',Georgia,serif;
    font-style:italic;
    font-size:16px;
    color:#8a857a;
  }

  /* Footer band */
  .lib-footer-band {
    background:#111110;
    border-top:3px solid #c8342a;
    padding:clamp(32px,4vw,56px) 0;
  }
  .lib-footer-inner {
    display:flex;
    flex-wrap:wrap;
    gap:24px;
    align-items:center;
    justify-content:space-between;
  }
  .lib-footer-label {
    font-family:'Oswald',sans-serif;
    text-transform:uppercase;
    letter-spacing:0.3em;
    font-size:10px;
    color:#8a857a;
    margin-bottom:6px;
  }
  .lib-footer-name {
    font-family:'Oswald',sans-serif;
    font-size:14px;
    text-transform:uppercase;
    letter-spacing:0.15em;
    color:#f6f3ec;
  }
  .lib-footer-link {
    font-family:'Oswald',sans-serif;
    text-transform:uppercase;
    letter-spacing:0.22em;
    font-size:10px;
    color:#f5c518;
    border-bottom:1px solid rgba(245,197,24,0.35);
    padding-bottom:1px;
    text-decoration:none;
  }
  .lib-footer-link:hover { border-bottom-color:#f5c518; }
</style>

<div class="lib-page">
  <div class="lib-stripe"></div>

  <!-- HERO -->
  <header class="lib-hero">
    <div class="lib-wrap">
      <div class="lib-hero-eyebrow">E5 Enclave · Living Memorial</div>
      <h1 class="lib-hero-title">Libations.</h1>
      <p class="lib-hero-deck">We pour for those who came before us, who fought alongside us, and who left before we were done. We are a moment in a movement — and the movement does not forget.</p>
      <div class="lib-stripe med"></div>
      <div class="lib-hero-creed" style="margin-top:24px;">Honoring the Fallen · Celebrating the Risen · Carrying the Work Forward</div>
    </div>
  </header>

  <!-- DOCTRINE BAND -->
  <section class="lib-doctrine">
    <div class="lib-wrap">
      <div class="lib-doctrine-inner">
        <div>
          <div class="lib-doctrine-label">What Is This Space</div>
          <h2 class="lib-doctrine-title">A Pour<br/>for Every<br/>Name.</h2>
        </div>
        <div class="lib-doctrine-body">
          <p>In African tradition, libations are poured to honor the ancestors — those who gave their lives so that we might live ours with more dignity, more power, and more possibility than they were afforded. This page is that act, made permanent.</p>
          <p>Here we recognize those who have fallen victim to the system we are working to dismantle, those who fought and won ground we now stand on, and those who carried the banner so far that they can no longer carry it themselves. Their names do not retire. They are promoted.</p>
          <p>We are a moment in a movement. Every name on this page is the reason the movement continues to move.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- TRIBUTE GRID -->
  <section class="lib-grid-section" style="background:#0a0a0a;">
    <div class="lib-wrap">
      <div class="lib-grid-header">
        <div class="lib-stripe short"></div>
        <div class="lib-grid-eyebrow">The Tributes</div>
        <h2 class="lib-grid-title">Names We<br/>Pour For.</h2>
      </div>

      <div class="lib-grid">
        <!-- FEATURED TRIBUTE — Andrew Joseph Jr. -->
        <a class="lib-card" href="/libations/andrew-joseph-jr/" style="grid-column: span 2;" aria-label="Andrew Joseph Jr. tribute page">
          <img src="/assets/img/libations/andrew-fist-raised.png"
               alt="Andrew Joseph Jr., Rapid Response Coordinator, Black Lives Matter Grassroots, raised fist raised in solidarity"/>
          <span class="lib-card-date">Transitioned May 16, 2026</span>
          <div class="lib-card-body">
            <div class="lib-card-stripe"><div class="lib-stripe short"></div></div>
            <div class="lib-card-eyebrow">In Memoriam · Inaugural Libation</div>
            <h2 class="lib-card-name">Andrew<br/>Joseph Jr.</h2>
            <div class="lib-card-role">Rapid Response Coordinator · Black Lives Matter Grassroots</div>
            <p class="lib-card-quote">"We've been boots on the ground since day one."</p>
            <span class="lib-card-cta">Read the full tribute →</span>
          </div>
        </a>

        <!-- PLACEHOLDER — Future tributes -->
        <div class="lib-card-placeholder">
          <div class="ph-mark">✦</div>
          <div class="ph-label">Future Libation<br/>Reserved</div>
        </div>
        <div class="lib-card-placeholder">
          <div class="ph-mark">✦</div>
          <div class="ph-label">Future Libation<br/>Reserved</div>
        </div>
      </div>
    </div>
  </section>

  <!-- CREED SECTION -->
  <section class="lib-creed">
    <div class="lib-wrap">
      <div class="lib-creed-inner">
        <div class="lib-stripe short" style="margin:0 auto 32px;"></div>
        <blockquote>"Those who have risen do not leave. They become the ground we stand on."</blockquote>
        <cite>— E5 Enclave · Libations Doctrine · May 2026</cite>
        <div style="margin-top:40px;">
          <a href="/coalition/" style="font-family:'Oswald',sans-serif;text-transform:uppercase;letter-spacing:0.25em;font-size:11px;color:#f5c518;border-bottom:1px solid rgba(245,197,24,0.4);text-decoration:none;padding-bottom:2px;">Join the Coalition of the Willing →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- FOOTER BAND -->
  <div class="lib-footer-band">
    <div class="lib-wrap">
      <div class="lib-footer-inner">
        <div>
          <div class="lib-footer-label">Published by</div>
          <div class="lib-footer-name">E5 Enclave Incorporated · EIN 99-3822441</div>
        </div>
        <div style="display:flex;gap:28px;flex-wrap:wrap;align-items:center;">
          <a class="lib-footer-link" href="/doctrine/">The Doctrine</a>
          <a class="lib-footer-link" href="/black-dragons-initiative/">BDI</a>
          <a class="lib-footer-link" href="/donate/">Support the Work</a>
        </div>
      </div>
    </div>
  </div>
  <div class="lib-stripe"></div>
</div>
